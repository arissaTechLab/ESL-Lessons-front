// 📤 Public API of the `landing` feature.
export { LandingPage } from './pages'
// Shared marketing sections — reused by other public pages (e.g. `about`,
// `pricing`).
export { CtaSection, TestimonialsSection } from './components'
