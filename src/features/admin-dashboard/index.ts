// 📤 Public API of the `admin-dashboard` feature.
export { AdminDashboardPage } from './pages'
// MetricCard is shared with the other admin reporting screens (admin-revenue).
export { MetricCard } from './components'
