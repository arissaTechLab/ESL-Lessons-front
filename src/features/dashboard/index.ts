// 📤 Public API of the `dashboard` feature (the two private zones).
export { ClientHomePage, AdminHomePage } from './pages'
export { dashboardService } from './services/dashboard.service'
export type {
  Subscription,
  Material,
  AdminDashboard,
} from './types/dashboard.types'
