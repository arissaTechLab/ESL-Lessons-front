# Copy to `.env` and fill in. Only variables prefixed with VITE_ are exposed
# to the client bundle. NEVER put secrets here — this file is committed.

# Base URL of the ESL Lessons backend, including the /api prefix.
VITE_API_URL=http://localhost:3000/api
